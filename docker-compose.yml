services:
  webapi-demo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: webapi-demo
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
    volumes:
      - ./database.db:/app/database.db
    restart: unless-stopped
